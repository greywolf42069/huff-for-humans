[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.28"
evm_version = "cancun"
fuzz = { runs = 256 }
# Test suites deploy the contract from the compiled creation bytecode on disk.
fs_permissions = [{ access = "read", path = "./bytecode.txt" }]

[fmt]
line_length = 100
tab_width = 4
